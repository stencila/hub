# Secret environment variables used in docker-compose.yaml
#
# The values below are obviously not secrets and
# SHOULD BE OVERIDDEN in production. They are place here,
# in a separate file to highlight that fact.

DATABASE_URL=postgres://stencila:password@database:5432/hub
POSTGRES_PASSWORD=password
